@model IEnumerable<BibliotecaWebb.Models.Autor>

<h1>Lista De Autores</h1>

<div class="table-responsive-vertical shadow-z-1">
    <table id="table" class="custom-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Libros</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var autor in Model)
            {
                <tr>
                    <td data-title="ID">@autor.AutorID</td>
                    <td data-title="Nombre">@autor.Nombre</td>
                    <td data-title="Libros">
                        @if (autor.Libros != null)
                        {
                            foreach (var libro in autor.Libros)
                            {
                                <span>@libro.Titulo</span><br />
                            }
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
